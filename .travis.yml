sudo: required
services:
  - docker

before_install:
  - docker build -t gli68/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true gli68/react-test npm test 

after_success:
  - docker build -t gli68/multi-client ./client
  - docker build -t gli68/multi-nginx ./nginx
  - docker build -t gli68/multi-server ./server
  - docker build -t gli68/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push gli68/multi-client
  - docker push gli68/multi-nginx
  - docker push gli68/multi-server
  - docker push gli68/multi-worker
